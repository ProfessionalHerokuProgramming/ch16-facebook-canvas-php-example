create table entries (
    id            serial,
    fb_id         varchar(255),
    first_name    varchar(255),
    last_name     varchar(255),
    date_of_birth varchar(255),
    gender        varchar(255),
    email         varchar(255),
    created       timestamp
);
